const ProjectTextBox = () => {
    return (
        <div>
            Inside ProjectContainer Look - images - title and description -
            round Contain - Image , can be changed by the user imported - Title
            - Short description - expandable if hovering for a short period
            Action - if clicked then to project pages
        </div>
    );
};

export default ProjectTextBox;

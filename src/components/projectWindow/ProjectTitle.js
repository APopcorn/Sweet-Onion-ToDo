import style from "./ProjectTitle.module.scss"

const ProjectTitle = () => {
  return <div className={style.project__title}>Project Title</div>;
  // whats to change color and text
};

export default ProjectTitle;
